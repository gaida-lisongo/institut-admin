# Variables d'environnement pour l'application

# URL de l'API backend (accessible côté client)
NEXT_PUBLIC_SERVER_API_URL=http://localhost:4000/api/v1

# URL de l'API backend (accessible côté serveur uniquement)
SERVER_API_URL=http://localhost:4000/api/v1
